"""Bundled QM helper scripts shipped with cypforge_core.

This subpackage is an internal implementation detail and is not part of the
public API. It exists so that wheel installs can locate the QM scripts via
`importlib.resources` instead of relying on the source-tree `scripts/`
directory.
"""
